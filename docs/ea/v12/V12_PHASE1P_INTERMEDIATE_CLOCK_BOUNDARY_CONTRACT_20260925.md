# V12 Phase 1P — intermediate-clock boundary contract

Status: **frozen before evaluation / consumed-development only**  
Frozen: `2026-09-25`

Phase 1O finds a useful after-stop temporal mechanism on H1 but not M15. Phase
1P fills the untested interval with H2 and M30 rather than assuming either
endpoint is optimal.

Both clocks use the same causal `k=1` Child, previous same-clock STD-HA Hard SL,
opposite FAST exit, after-stop cohort, history fields, fixed HGB parameters,
walk-forward folds, top-repeat-risk quintile, and capital gates as Phase 1O.
H2 receives internal M30/M5 formation and H4 context. M30 receives internal M5
formation and completed H1/H4 context. Both receive the same continuous clock,
economic-event, session-source-box, CRT Parent, and Wave coordinates.

All ten Phase-1O ablations are rerun. No clock, model, feature family, threshold,
side, session, or event may be selected after seeing results. This phase grants
no trade or sizing authority. All observations through `2026-09-18 23:57` are
consumed; GOLD# 2021 and later chronology remain sealed.
