# V8 Research State

Status: `ACTIVE / PRE-IMPLEMENTATION`
Date: `2026-08-30`
Current phase: `V8-001A CAUSAL CHART REPRESENTATION FOUNDATION`
Production authority: `NONE`

## Phase map

### V8-000 — Representation reset
`COMPLETE`

Frozen decisions:

- V8 opened.
- V7 paused/preserved.
- ambiguous human market-state labels are not base decision authority;
- observable anchor and contextual meaning are separated;
- hybrid visual + numerical representation is the initial research family;
- campaign accounting is mandatory.

### V8-001A — Causal representation foundation
`ACTIVE`

Build:

- M1/M5/M15/H1 causal streams;
- deterministic chart renderer;
- overlay/event timing;
- visual/numerical timestamp parity;
- campaign-state schema;
- source manifest;
- no-future tests.

No P/L optimization.

### V8-001B — Anchor census + retrieval audit
`PENDING`

- freeze initial anchor definitions;
- build event ledger;
- compare latent neighbors and future-path coherence;
- inspect whether visual representation adds information beyond numerical-only input.

### V8-002 — Representation tournament
`PENDING`

Compare:

```text
R0 scalar baseline
R1 numerical sequence
R2 visual chart
R3 fused visual + numerical
```

Use chronological future-hidden development diagnostics.

### V8-003 — Action-conditioned future-path model
`PENDING`

No complex RL until representation value is demonstrated.

### V8-004 — Sequential campaign controller
`PENDING`

Explicit costs, exposure, add/reduce/exit lifecycle.

### V8-005 — Freeze
`PENDING`

Freeze preprocessing, representation, event population, model selection, action policy and risk architecture.

### V8-006 — Untouched GOLD# 2021 validation
`LOCKED`

Do not open early.

## Current evidence status

GOLD# 2022-2026 is open development evidence.

The unified M1 file currently available outside Git contains the full 2022-01-03 through 2026-08-28 period.

V7 findings remain historical semantic evidence but do not impose V8 archetype labels.

## Current null hypothesis

After strict causal representation and sequential accounting, a fused chart model may fail to provide
economically useful information beyond simpler numerical baselines.

V8 must be willing to accept this null rather than rescue the model with hindsight labels or threshold mining.
