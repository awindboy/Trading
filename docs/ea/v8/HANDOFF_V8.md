# V8 Development Handoff

Last updated: `2026-09-05`  
Current phase: `GOLD P15 -> SCENARIO PORTFOLIO / GLASS-CEILING -> P15-CENTERED CAUSAL STATE-TRANSITION & PERSISTENCE AUDIT`  
Production authority: `NONE`  
Market: `GOLD# ONLY`  
Untouched reserve: `GOLD# 2021`  
Source Git HEAD before this synchronization: `eb0daed45f8c2784d28599b954faba3ce2cd246c`

## 1. Resume order

On every next V8 session:

1. refresh Git HEAD;
2. read `AGENTS_V8.md`;
3. read this file;
4. read `RESEARCH_STATE_V8.md`;
5. read `V8_SCENARIO_GLASS_CEILING_RESEARCH_PRINCIPLES_20260904.md`;
6. read `V8_EXTERNAL_DIRECTION_RESEARCH_SYNTHESIS_20260905.md`;
7. execute/follow `V8_NEXT_P15_STATE_TRANSITION_PERSISTENCE_CONTRACT_20260905.md`;
8. read `V8_SCENARIO_LAYER_PHASE0_RESULT_20260904.md`;
9. read `V8_DIRECTIONAL_CHANGE_GLASS_CEILING_AUDIT_20260904.md`;
10. read `DECISIONS_V8_SCENARIO_GLASS_CEILING_ADDENDUM_20260904.md`;
11. use older V8/V3 documents only for preserved history or direct comparison.

Do not resume from conversation memory when GitHub has newer state.

## 2. Data/population authority

Current execution-development control:

- reproducible old P0 2024 fresh75 `N=648`;
- prior synchronized `653` remains historical and must not be silently mixed with 648;
- authoritative M1 source SHA256: `626d81d3d6ba94ac80d00748fa83e11ff5ec90df7fb6c98688c77f20d1604ff2`;
- 2024 exact Bid/Ask tick = development execution evidence;
- known active-coverage gaps are right-censored where M1 proves trading through the missing interval;
- 2025 exact tick = unavailable;
- 2025/2026 M1 = screening/descriptive robustness only for the current scenario/state-transition layer;
- GOLD# 2021 remains locked.

## 3. Stable upstream conclusions

Do not reopen without a genuinely new contradiction:

- P15 predicts near-term movement opportunity better than broad initial direction;
- movement onset is more learnable than generic current-state direction;
- ACCEPTANCE is structural context, not automatic Entry permission;
- High-Q is sparse continuation evidence, not a broad direction gate;
- generic direct direction classifiers, reveal FOLLOW/FADE, every-M1 EV, micro first-touch, stop-and-reverse, entry straddles and continuous M1 trend following are failed/downgraded;
- static ATR-grid sizing/deep-action variants did not solve the direction/payoff problem;
- exact Bid/Ask chronology remains mandatory for execution authority;
- denominator collapse is not accepted as a solution.

## 4. Permanent V3 lesson

V3 remains a critical scenario-design reference.

Preserve:

    higher auction/delivery state
    x local event/reaction
    x acceptance/failure

Do not preserve automatically:

- exact V3 swing logic;
- mandatory sweep/retest chain;
- exact timeframe chain;
- exact acceptance threshold;
- serial filters that recreate 20-40 trades/year.

V3 Candidate B's frozen 2022 validation failure remains a mandatory warning against defending discovery results.

## 5. Current Scenario Layer Phase-0 evidence

Three distinct development modules remain worth preserving:

### A. BB persistent expansion + HTF alignment

2024 exact tick:

- candidates 37;
- completed 32;
- WR 46.9%;
- mean +0.0435R;
- PF ~1.10;
- average positive +1.040R.

### B. Endogenous late ignition

2024 exact tick:

- candidates/completed 28;
- WR 60.7%;
- mean +0.134R;
- PF ~1.36;
- average positive +0.839R.

M1 same-payoff screening remains positive in 2024/2025/2026 but is not exact-execution validation.

### C. Scheduled major macro reaction

2024 exact tick:

- candidates 35;
- completed 33;
- WR 57.6%;
- mean +0.120R;
- PF ~1.31;
- average positive +0.887R.

Macro calendar clearly adds movement information absent from chart-only state. Direction remains a separate surprise/reaction/acceptance problem.

## 6. Current 2024 exact-tick scenario portfolio

Deduplicating to one causal module per P15 campaign:

- unique candidates 96;
- completed 89;
- censored 7;
- positive 49;
- negative 40;
- WR `55.06%`;
- mean `+0.1049R`;
- total `+9.339R`;
- PF `1.262`;
- average positive `+0.9168R`;
- average negative `-0.8896R`;
- +1R checkpoints 47;
- residual +2R hits 19.

This is about 3.3x the 2024 count of strict V3 Candidate-A.

Interpretation:

> Scenario quality and frequency may coexist through an orthogonal portfolio rather than by loosening one pattern.

But this remains development evidence only. Average winner is still below the project target and there is no independent exact-tick validation.

Do not retune 2024 to force the missing metric.

## 7. Current payoff control

The current scenario payoff control separates survival from continuation:

    initial risk = 0.25S
    +1R survival checkpoint
    -> realize 50%
    -> residual stop to BE
    -> residual target +2R

Checkpoint continuation decays materially beyond +2R.

Do not extend the runner simply to increase average winner.

The whole-position +0.5R floor variant underperformed and is rejected as an improvement.

## 8. Completed Directional Change glass-ceiling audit

Status:

`DIRECTIONAL CHANGE AS BROAD DIRECTION GATE = REJECTED / DOWNGRADED`

Natural scales:

- 0.125S;
- 0.25S;
- 0.50S.

Key result:

3-scale aligned direction outside existing modules was approximately:

- 2024: 49.8%;
- 2025: 54.2%;
- 2026: 48.2%.

DC state itself was ~99.7-100% stable across natural 240/480/960-minute initialization windows.

Interpretation:

> DC is a stable price representation but does not add a stable broad directional edge.

Do not optimize DC thresholds or add DC alignment to current modules. DC may remain shadow path representation only.

## 9. New external-research synthesis

External literature has been verified and documented in `V8_EXTERNAL_DIRECTION_RESEARCH_SYNTHESIS_20260905.md`.

Permanent rule:

> External research supplies falsification references and research questions, not V8 trading rules.

Important updated insight:

- short-horizon current-state/next-bar direction can have a thin gross-edge ceiling;
- this does **not** prove all price-only information is exhausted;
- a different price-only target remains legitimate: disturbance -> state transition -> persistence;
- external information such as macro surprise and COMEX flow remains a separate information-expanding lane.

The Mesfin (2026) MNQ paper is especially useful as a falsification example, but it is a single-author preprint on another instrument. Do not copy its GMM, Markov, session, threshold or hold parameters.

## 10. Current primary research hypothesis

The next question is no longer:

> Can we predict the next GOLD candle/direction better?

It is:

> After P15 detects a movement disturbance, does the market transition into a persistent state whose direction, distance and lifetime are stable enough to produce economic edge beyond friction?

Conceptual architecture:

    pre-event market state
    -> P15 movement disturbance
    -> causal transition path
    -> post-event state
    -> persistence / failure
    -> scenario-specific direction-distance-horizon
    -> survival checkpoint
    -> separate continuation/payoff

Static state labels have no Entry authority.

## 11. Why this is distinct from old t10/confirmation research

Old pattern:

    wait a fixed number of minutes
    -> select a smaller good-looking subset

New research target:

    measure whether a market-state transition actually occurred
    -> whether that state persists
    -> when it became causally knowable

Time is an observation coordinate, not the reason for permission.

If the result simply recreates `wait 10 minutes + narrow filters`, reject it as V3/t10 scarcity in new language.

## 12. Immediate next research contract

Follow `V8_NEXT_P15_STATE_TRANSITION_PERSISTENCE_CONTRACT_20260905.md`.

Stage 1 is shadow-only.

Required first work:

1. build a compact causal descriptor set for activity, directional efficiency, balance/overlap, value migration and existing higher state;
2. describe pre-P15 -> post-P15 transition paths without P/L gating;
3. optionally use a small outcome-blind unsupervised partition, with state semantics frozen by descriptor meaning rather than cluster number;
4. measure persistence over natural ~15/30/60m horizons before payoff design;
5. compare transition information against static-state, momentum/breakout and existing-module controls;
6. test 2024 H1/H2 and 2025/2026 M1 descriptive invariance;
7. test whether Late Ignition, BB Persistence and Macro are one common transition mechanism or genuinely distinct causes;
8. only after the relationship survives glass-ceiling checks, define a causal executable transition timestamp and escalate to 2024 exact tick.

Do not touch 2021.

## 13. Parallel macro lane

Macro remains the main information-expanding candidate:

    scheduled release
    -> pre-event state
    -> actual-consensus surprise when causally known
    -> first reaction
    -> acceptance/failure

Do not merge macro surprise into the chart-only state-transition model without explicit lane separation.

## 14. Research stop rules

Do not:

- invent another static next-bar classifier in regime language;
- copy GMM/Markov thresholds from outside research;
- optimize cluster count from P/L;
- extend holding horizon before proving persistence;
- add DC alignment as a generic gate;
- call several parameter variants independent modules;
- serially stack confirmations until trade count returns to V3 scarcity;
- treat 2025 M1 as exact execution validation;
- tune 2024 exact tick until every project target passes;
- repair validation failure by moving thresholds;
- use GOLD# 2021.

## 15. Production status

`NONE`
