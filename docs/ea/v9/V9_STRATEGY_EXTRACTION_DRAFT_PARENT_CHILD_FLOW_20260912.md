# V9 Strategy Extraction Draft — Parent / Child Continuous Flow

Date: `2026-09-12`
Status: `FROZEN CONSUMED-DATA STRATEGY-EXTRACTION DRAFT / NOT PRODUCTION AUTHORITY / NOT EA AUTHORITY`
Market: `GOLD# ONLY`
Consumed answer-sheet data: `2025-01 through 2025-06`, `2026-01 through 2026-02`
Future-hidden: `2025-07 LOCKED`
Untouched reserve: `GOLD# 2021`
Base GitHub HEAD researched: `e3355d6c0de311561390ae4f9ba729f2617d3c35`
Authoritative M1 SHA256: `626d81d3d6ba94ac80d00748fa83e11ff5ec90df7fb6c98688c77f20d1604ff2`

## 1. Purpose and authority boundary

This document freezes the first strategy-extraction draft from the continuous hierarchical market-flow grammar.

It does **not** claim a validated live edge, production readiness, EA authority, or permission to open future-hidden July.

The draft exists to define the smallest causal decision process that is currently supported by consumed-data research without returning to setup filtering or outcome fitting.

The market model remains:

```text
H4 MACRO AUTHORITY
-> H4 PHASE
-> H1 AUCTION
-> M15 local flow evidence
-> M5 execution geometry
-> Child outcome / journey review
-> REMAP or RESET
```

Parent is a scale/context authority. Parent is **not** a mandatory trade-direction veto.

## 2. Continuous state coverage is not a rare-setup funnel

The research must not be interpreted as reducing thousands of candidates to a tiny historical subset.

The active-Parent H1 interruption universe is partitioned by state transitions.

Current migration-anchored Parent topology:

```text
H1 cycles inside ACTIVE Parent: 171
  SAME_PARENT_REALIGN: 120
  PARENT_LOSS_FIRST:    51
```

After same-Parent H1 realign, all 120 routes partition naturally into:

```text
H1 launch anchor never touched:              63
anchor touched, no completed M15 close loss: 26
completed M15 close beyond launch anchor:     31
TOTAL:                                       120
```

These are route states, not accepted/rejected setup counts.

## 3. With-Parent repair / continuation branch

### 3.1 Authorization

Inside an ACTIVE Parent:

```text
H1 INTERRUPT
-> WAIT
-> fresh M15 transition back into Parent side
-> WITH-PARENT CHILD PITCH AUTHORIZED
```

Consumed active-Parent cycles with this first M15 reauthorization:

```text
94 total
  81 eventual SAME_PARENT_REALIGN
  13 eventual PARENT_LOSS_FIRST
```

This percentage is descriptive topology, not a forward win rate.

The M15 event is authorization to build a Child pitch. It is not automatic entry.

### 3.2 Falsification authority

The meaningful structural falsification candidate is the adverse extreme created during the current H1 interruption before authorization.

Call this:

```text
INTERRUPTION_ORIGIN_BOUNDARY
```

M5 confirmed swings often annotate the same boundary, but M5 swing existence is not mandatory authority.

After correcting M5 object availability to information-known time, a matching/near-matching causal M5 origin swing existed in `83 / 94` first reauthorization cases. Therefore requiring the M5 swing would unnecessarily discard valid market states.

Hard-SL principle remains:

```text
Hard SL fixed before entry
never widened
stopped Child is dead
later movement cannot rescue it
```

The exact executable Hard-SL coordinate remains runtime/code-owned.

### 3.3 M5 execution role

M5 object existence is too common to authorize direction.

Dual-clock-corrected first-pitch research:

```text
94 M15-authorized pitches
90 had a causally available Parent-direction M5 FVG/OB location
69 filled before H1/Parent terminal under the simple precommitted-location research model
```

Therefore:

```text
M15 = pitch / direction-scale authorization
M5  = execution location and local geometry
```

Do not use:

```text
M5 FVG exists -> trade
M5 OB exists -> trade
zone touch -> pitch quality proven
M5 confirmation chain -> mandatory trigger
```

No fixed order expiry, cooldown, retry cap, minimum-R, ATR target, or no-chase threshold is authorized.

### 3.4 H1 realign and journey promotion

If a paid Child survives its frozen origin boundary until H1 realigns with the Parent:

```text
H1 REALIGN
-> Child may enter PARENT-JOURNEY participation phase
```

H1 realign is not a default TP and is not a required delayed entry.

The completed H1 realign bar creates a natural higher-scale journey review boundary:

```text
H1_REALIGN_LAUNCH_ANCHOR
= adverse edge of the completed H1 realign bar
```

Across all 120 same-Parent routes:

```text
NO_TOUCH:             63
TOUCH_ONLY:           26
M15_CLOSE_BEYOND:     31
```

Among the 31 completed M15 close-damage routes, `28 / 31` made no new Parent-favorable peak before the route boundary.

Across alternate H1 views the same no-new-peak topology after completed M15 close damage remained approximately `86% to 97%` depending on view.

This supports the semantic distinction:

```text
M1 touch of H1 launch anchor
-> REVIEW / possible damage

completed M15 close beyond H1 launch anchor
-> MATERIAL CHILD-JOURNEY DAMAGE
```

Do not promote this into a claim that Parent is dead.

### 3.5 Delivery and counter landmarks

Retain:

```text
WITH-PARENT liquidity delivery
= progression / route milestone
!= automatic TP
!= automatic trailing authority

counter POI / counter liquidity
= landmark / review context
!= automatic exit
```

Mechanically ratcheting a review anchor after every delivery or every M15 reauthorization reduced large-tail participation in consumed research. Do not authorize such trailing rules.

### 3.6 Child exit and Parent remap

Current causal interpretation:

```text
Child launch / journey structure materially fails
-> EXIT current Child
-> Parent may remain ACTIVE
-> REMAP
-> wait for fresh objective information
```

If a stopped Child later receives a fresh M15 Parent-side reauthorization while Parent remains active, a new independent Child pitch may be built.

Do not count retries. Do not use cooldowns. The new pitch exists only because a new objective market fact exists.

## 4. Counter-Parent Local-Bridge branch

Parent is not a direction veto. A local counter move can be a valid Child even while the slower Parent remains active.

### 4.1 Natural precursor topology

Before H1 enters interruption, M15 often turns counter first while H1 is still Parent-aligned.

Using the strict rule `fresh transition must occur before H1 interruption known_at`:

```text
ACTIVE-Parent H1 cycles: 171
fresh M15 counter transition before H1 interruption: 141
M15 already counter at aligned-run start:             17
no counter condition in that aligned run:              13
```

The older exploratory count that included boundary/same-run re-entry variants must not be used as authority; `141` is the strict pre-H1 fresh count for this draft.

Fresh-counter incidence is similar in later same-Parent repair and Parent-loss outcomes. Therefore the M15 counter transition is not a Parent-reversal predictor.

Its role is:

```text
H1 still ALIGNED
-> fresh M15 counter transition
-> COUNTER LOCAL-BRIDGE CHILD PITCH AUTHORIZED
```

### 4.2 Counter pitch falsification and M5 role

The structural origin boundary is the Parent-direction extreme of the current H1-aligned run up to the fresh counter M15 authorization.

Call this:

```text
COUNTER_ORIGIN_BOUNDARY
```

In `125 / 141` strict fresh counter pitches, the origin boundary was exactly represented by a causally confirmed M5 swing. Do not require that M5 swing in the remaining cases; the higher-scale run-origin boundary is the semantic falsification authority.

M5 FVG/OB location is again common:

```text
141 strict fresh counter pitches
136 had a causally available counter-direction M5 FVG/OB location
119 filled in the simple precommitted-location research model
```

Therefore M5 remains execution geometry, not direction authority.

### 4.3 H1 interruption is counter-bridge progression, not Parent death

Most filled counter pitches were entered before H1 interruption:

```text
119 fills
95 before H1 interruption
24 after H1 interruption while the frozen pitch remained structurally alive
```

At H1 interruption, freeze the adverse boundary of the completed H1 interruption bar:

```text
H1_INTERRUPT_LAUNCH_ANCHOR
```

Across all 171 active-Parent cycles:

```text
NO_TOUCH:             108
TOUCH_ONLY:            20
M15_CLOSE_BEYOND:      43
```

By eventual higher-level outcome:

```text
PARENT_LOSS_FIRST 51:
  NO_TOUCH             45
  TOUCH_ONLY            2
  M15_CLOSE_BEYOND      4

SAME_PARENT_REALIGN 120:
  NO_TOUCH             63
  TOUCH_ONLY           18
  M15_CLOSE_BEYOND     39
```

Thus H1 interrupt launch-anchor integrity describes Counter-Child coherence; it is not a direct Parent forecast rule.

After completed M15 close damage, `37 / 43` cycles made no new counter-favorable peak before their H1/Parent terminal.

All 6 counterexamples that did make a new favorable peak first produced a fresh M15 counter reauthorization after the old launch damage. Therefore do not rescue the old Child. Use:

```text
old Counter Child damaged
-> EXIT / REMAP old Child
-> fresh counter reauthorization may create a NEW independent pitch
```

### 4.4 Counter Child stop and reauthorization

Among the simple first counter fills, 59 hit their original structural origin before the current H1/Parent terminal.

```text
fresh counter reauthorization after stop: 51 / 59
```

All 16 stopped first Children in eventual Parent-loss cycles later produced a fresh counter reauthorization before terminal.

This is evidence for objective re-pitching, not a retry-count rule.

### 4.5 Handoff when the old Parent loses authority

A Counter Child that remains structurally alive when the old Parent ends requires mandatory remap; Parent loss alone does not rewrite the Child outcome.

In the current simple first-fill research, 15 Counter Children survived to old-Parent loss.

The next migration-anchored Parent was:

```text
opposite / Counter-Child direction: 11
same as old Parent re-earned:        4
```

All `11 / 11` opposite-new-Parent cases retained the original Counter-Child origin until the new Parent was earned.

This supports a causal handoff candidate:

```text
COUNTER LOCAL-BRIDGE
-> old Parent authority lost
-> opposite MIG earns new Parent while Child remains alive
-> Child may be reclassified as WITH-NEW-PARENT JOURNEY
```

The 4 same-side re-earn cases remain an explicit unresolved remap state. Do not create a special rule from this small subset.

## 5. Unified Child state machine

### 5.1 Core market states

```text
RESET
  no active migration-anchored Parent

PARENT_ACTIVE
  H1_ALIGNED
  H1_INTERRUPT

CHILD_NONE
CHILD_PENDING
CHILD_OPEN_LOCAL_BRIDGE
CHILD_OPEN_PARENT_JOURNEY
CHILD_EXITED_REMAP
```

### 5.2 With-Parent branch

```text
Parent ACTIVE
-> H1 INTERRUPT
-> fresh M15 Parent-side reauthorization
-> freeze interruption-origin falsification
-> map M5 execution location
-> pending Child

pre-fill origin invalidation / material context damage
-> cancel

location fill
-> Child OPEN

origin invalidation
-> Child DEAD / REMAP Parent

H1 REALIGN while Child alive
-> PARENT-JOURNEY review phase
-> freeze H1 realign launch anchor

launch-anchor touch
-> REVIEW

completed M15 close beyond launch anchor
-> MATERIAL CHILD DAMAGE / EXIT-REMAP candidate
```

### 5.3 Counter branch

```text
Parent ACTIVE + H1 still ALIGNED
-> fresh M15 counter transition
-> freeze aligned-run counter-origin boundary
-> map M5 execution location
-> COUNTER LOCAL-BRIDGE Child pitch

H1 INTERRUPT
-> local-bridge progression confirmed
-> freeze H1 interrupt launch anchor

launch-anchor touch
-> REVIEW

completed M15 close beyond launch anchor toward Parent side
-> material Counter-Child damage / EXIT-REMAP candidate

old Parent authority lost while Counter Child remains alive
-> mandatory REMAP
-> opposite new Parent earned
   -> possible WITH-NEW-PARENT JOURNEY promotion
```

## 6. What is explicitly NOT authorized

Do not introduce:

- fixed minimum-R;
- fixed ATR/S stop or target;
- fixed timeout / order expiry;
- cooldown;
- retry cap;
- fixed no-chase distance;
- forced LONG/SHORT balance;
- trades/day quota;
- M5 FVG/OB existence as direction authority;
- mandatory `zone touch -> M5 confirmation -> entry` chain;
- H1 3/3 agreement as automatic hold gate;
- delivery as TP or automatic trailing authority;
- M15 damage alone as automatic Parent death;
- counter POI/liquidity as automatic exit;
- hindsight recovery of a stopped Child.

## 7. Remaining unresolved items

This draft intentionally leaves unresolved:

1. exact broker/tick fill ordering inside one M1 bar;
2. exact executable entry price policy for zone touch under OHLC-only history;
3. whether completed M15 launch-anchor damage is mandatory exit authority or a deterministic mandatory-review event in final live design;
4. the 4 observed same-side Parent re-earn handoffs after a surviving Counter Child;
5. maximum-staleness fail-safe design;
6. final AI call/remap packet format;
7. costs, spread, slippage and live execution effects;
8. future-hidden validation.

Do not solve these by mining thresholds from consumed data.

## 8. Next gate

Before `2025-07` can be considered for unlock:

```text
1. implement dual-clock semantic runtime;
2. implement M15/M5 object availability using information-known time;
3. reproduce this frozen strategy draft sequentially on consumed periods;
4. verify pending-order cancellation / Hard-SL / review event priority;
5. freeze implementation version and hashes;
6. only then decide whether the future-hidden replay gate is satisfied.
```

`2025-07` remains locked at this checkpoint.

## 9. 2026-09-12 causal implementation clarification

<!-- V9_CAUSAL_STATE_MACHINE_IMPL_20260912 -->

The first strategy draft has now been implemented without conditioning live authorization on future H1-cycle membership.

Retain the older answer-sheet counts for their original descriptive question:

```text
Counter fresh transition inside later resolved cycle context: 141
With-Parent first reauthorization inside later resolved cycle context: 94
```

Runtime authority must use only contemporaneously knowable context:

```text
live-safe first Counter authorization: 149
live-safe first With-Parent authorization: 95
```

This distinction is not a new strategy filter. It removes a hidden future-cycle conditioning dependency.

Additional implementation semantics now frozen for testing:

```text
H1 strategy context: ALIGNED / INTERRUPT
Atlas gap segmentation != trade timeout
full same-known-at batch before authorization
M5 object SOURCE_LAST_M1_AT != OBJECT_KNOWN_AT
zone touch -> ENTRY_EXECUTION_REQUIRED
review EXIT -> EXIT_EXECUTION_REQUIRED
Parent loss on alive paid Child -> REMAP_REQUIRED
cross-lane conflict -> fail-closed review
```

No fill, exit, hedge, remap, or AI decision is invented from an old completed-bar price.

The full implementation checkpoint and unresolved items are in:

`V9_CAUSAL_STATE_MACHINE_IMPLEMENTATION_CHECKPOINT_20260912.md`

This does not authorize `2025-07`.

## 10. 2026-09-12 validated AI interface clarification

<!-- V9_AI_EXTERNAL_DECISION_VALIDATED_20260912 -->

The earlier unresolved `AI call/remap packet format` is resolved only at the deterministic gate-envelope / scheduler / response-validation level. The consumed 594-gate path passed exactly.

The strategy-quality choice among `HOLD / EXIT / REMAP` remains unresolved, and the chart-native AI MAP input remains to be frozen. No strategy threshold was added and the `149 Counter / 95 With-Parent` live-safe authorization universe is unchanged.
