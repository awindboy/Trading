# V9 Next Research Contract — Post Topology/Journey Validation

Date: `2026-09-14`
Status: `ACTIVE NEXT CONTRACT`
Base checkpoint: `V9_LIQUIDITY_TOPOLOGY_AND_JOURNEY_VALIDATION_CHECKPOINT_20260914.md`
Production authority: `NONE`
EA authority: `NONE`
Future-hidden: `2025-07 LOCKED`
Untouched reserve: `2021`

## Objective

Convert the strongest consumed mechanical candidate into a frozen, reproducible policy without adding new entry-pattern optimization.

Current candidate core:

```text
PRIMARY_ACTIVE
-> immediate causal Child entry
-> H1 structural Hard SL
-> do not full-TP at first same-side H4 liquidity
-> first opposite H4-liquidity arrival opens CHALLENGED
-> CHALLENGED becomes mandatory Child-management boundary
```

`SAME_NEAREST` remains a factual continuation-quality attribute, not a mandatory gate.

## Required sequence

```text
1. Freeze a standalone reproduction script and hashes.
2. Reproduce 2024 / 2025H1 / 2026JF byte-for-byte from consumed-safe source ranges.
3. Resolve exact CHALLENGED action:
   full exit vs partial realization vs mandatory review.
4. Keep Hard SL fixed before entry and never widen.
5. Study multi-Child/exposure separately from market edge.
6. Add spread/cost sensitivity without mining a spread threshold.
7. Implement candidate through the dual-clock runtime.
8. Prove restart/event/object/SL/journey parity.
9. Review hidden gate only after the mechanical/runtime packet is frozen.
10. AI additive-value study remains deferred.
11. LTF entry-trigger research remains deferred.
```

## Prohibited

Do not add:

- minimum R;
- fixed ratio threshold;
- cooldown;
- retry cap;
- trade/day quota;
- forced LONG/SHORT balance;
- fixed no-chase;
- duration timeout;
- future-born structure to retroactively change the original SL;
- hindsight recovery of stopped Children.

## Completion criterion

The contract is complete only when the project can reproduce the consumed candidate from raw causal inputs and state exactly how an open Child is managed at `CHALLENGED`, with deterministic runtime parity and cost sensitivity documented.
