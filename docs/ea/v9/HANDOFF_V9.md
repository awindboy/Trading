# V9 Development Handoff

Last updated: `2026-09-08`  
Status: `ACTIVE V9`  
Current phase: `PARENT JOURNEY CONTINUITY -> CHILD OPPORTUNITY REPLAY -> FEBRUARY CAUSAL COMPLETION`  
Production authority: `NONE`  
EA authority: `NONE`  
Market: `GOLD# ONLY`  
Untouched reserve: `GOLD# 2021`  
Expected Git base for this update: `14e1a31d876f220c237223607d7d651584cd7703`  
Authoritative M1 SHA256: `626d81d3d6ba94ac80d00748fa83e11ff5ec90df7fb6c98688c77f20d1604ff2`

## 1. Resume order

Every new V9 research session must:

1. refresh latest GitHub HEAD;
2. read root `AGENTS.md`;
3. read `docs/ea/HANDOFF.md`;
4. read `docs/ea/v9/AGENTS_V9.md`;
5. read this file;
6. read `docs/ea/v9/RESEARCH_STATE_V9.md`;
7. read `docs/ea/v9/V9_TRADING_MINDSET_AND_RESEARCH_GUARDRAILS_20260907.md`;
8. read `docs/ea/v9/V9_MANUAL_CHART_REPLAY_MARKET_MEMORY_AND_DECISIONS_20260907.md`;
9. read existing V9 decision documents;
10. read `docs/ea/v9/V9_PAPER_TRADING_JOURNAL_20260907.md`;
11. read `docs/ea/v9/V9_PARENT_CHILD_REPLAY_202502.md`;
12. read `docs/ea/v9/V9_FEB25_CAUSAL_REPLAY_ADDENDUM_20260908.md`;
13. use relevant result documents under `docs/ea/v9/results/`;
14. inspect authoritative raw data before advancing future-hidden replay.

GitHub is the Single Source of Truth. Chat history is a workbench.

---

## 2. Permanent V9 separation

```text
market understanding
!=
direction prediction
!=
good trade
```

V9 is not trying to predict the final top, bottom, direction, or trend length with certainty.

The practical objective is:

```text
recognize a worthwhile pitch
+
enter from a location with meaningful falsification
+
accept bounded failure
+
stay available for another independent opportunity
+
participate farther when the market genuinely permits it
```

The baseball-hitter principle remains the governing mindset.

---

## 3. Stable market-reading principles

Retain:

```text
Role > Pattern
boundary crossing != value translation
reaction != rejection
destination != reversal
direction correctness != trade quality
later same-direction movement cannot rescue an invalidated child thesis
invalidated child thesis != invalidated parent journey
winning child trade != proof of parent direction
```

Also retain:

- adaptive M5 -> M15 -> H1 -> H4 -> D1 history compression;
- sparse active market memory;
- Parent Journey / Child Trade hierarchy;
- counterfactual thesis dependence;
- failed repair as process language rather than a named setup;
- NO TRADE / NOT YET as valid decisions;
- no hindsight rescue;
- structure first, volatility scale second.

---

## 4. Evidence boundary

Market:

```text
GOLD# ONLY
```

Evidence status:

- 2024 exact Bid/Ask tick remains development execution evidence;
- 2025 exact tick is unavailable;
- 2025 M1 replay is descriptive where Bid/Ask or intraminute ordering matters;
- 2024-2026 are heavily consumed development evidence;
- none of the current V9 February evidence is untouched OOS;
- `GOLD# 2021` remains locked final temporal reserve.

No current replay result should be reported as validated production expectancy.

---

## 5. January findings retained

### January 2026

Seven discretionary development trades:

```text
six destination-resolved positives
one weekend-gap / execution-censored failure
```

Six destination-resolved examples summed to approximately `+10.38R` descriptively.

This is not validation authority.

### January 2025

One actual child trade:

```text
LONG ~2653.74
falsification ~2649-2650
exit ~2649.32
~ -1.04R descriptive
```

The later rally does not rescue the child.

It did demonstrate that:

```text
failed child thesis
!=
failed larger journey
```

---

## 6. H4 ATR coordinate

Use:

```text
S(t) = previous fully completed H4 Wilder ATR14
```

S is a distance coordinate only.

Never mechanically infer:

```text
SL = kS
TP = kS
trend switch = kS
```

High R and large market participation remain separate concepts.

---

## 7. Exit control retained

The existing CP1 full-exit control remains a comparison control.

Prior neutral 50/50 runner audit:

```text
Control A sum +8.861R
Challenger C +4.758R
C - A       -4.104R
```

Passive runner logic was not promoted.

The unresolved issue is `CAPTURE LATENCY`, not simply runner percentage.

---

## 8. Parent Journey remains continuity context

Parent Journey exists so a resolved child trade does not erase the larger market story.

It is a sparse working belief, for example:

```text
upper journey remains more coherent
recent downside action materially damaged it
damage has partly repaired
opposing route has not earned durable control
uncertainty remains
```

Parent Journey must NOT become:

```text
UPTREND => only LONG
DOWNTREND => only SHORT
```

Counter-parent trades remain valid when the local pitch is independently good.

---

## 9. February 2025 current evidence

Detailed source:

`V9_PARENT_CHILD_REPLAY_202502.md`

Current addendum:

`V9_FEB25_CAUSAL_REPLAY_ADDENDUM_20260908.md`

Current causal interval:

```text
2025-02-03
through
2025-02-20 10:30
```

Current child state:

```text
PAPER-001 ... PAPER-015 CLOSED
PAPER-016 OPEN
```

Closed descriptive decision outcomes:

```text
8 structural positive
7 structural loss
```

Do not call this a validated `53.3% win rate`.

Exact execution authority is absent.

---

## 10. What February changed

The major improvement is:

```text
CONTINUITY ARCHITECTURE
```

V9 increasingly behaves like:

```text
Parent Journey
  -> child attempt
  -> child thesis resolves
  -> parent updated independently
  -> wait for another good pitch
  -> take a new child if independently justified
```

rather than:

```text
local trade ends
-> larger story effectively resets
```

This increased opportunity participation without forcing frequency.

`2025-02-17` produced zero trades despite substantial movement.

---

## 11. Critical February stress cases

### PAPER-003 -> 004 -> 005 -> 006

Several LONG children failed.

Each invalid child was closed.

The larger story accumulated damage rather than being automatically killed.

Subsequent market repair created a genuinely new opportunity.

PAPER-006 then became approximately a `+3R`, `+1.48S` structural runner.

This remains the clearest child/parent separation example.

### PAPER-008 / 009

Both local SHORTs were positive despite meaningful upper Parent Journey context.

The market later expanded sharply upward.

Therefore:

```text
good SHORT child
!=
bearish parent proven
```

### PAPER-011

Large favorable excursion was mostly surrendered before structural failure was confirmed.

This remains the primary capture-latency counterexample.

---

## 12. OPEN ROUTE state

Closed OPEN ROUTE examples currently include:

```text
Positive:
006
010
011

Loss:
003
013
014
015
```

The sample is small and mixed.

Do not freeze an OPEN ROUTE hit rate or payoff model.

The PAPER-013-015 cluster raises a specific falsification concern:

> Was the apparent new launch anchor genuinely a new market-earned thesis, or was a failed breakout thesis being renamed and revived?

This is a role question, not a retry-count question.

Do NOT add:

- retry limits;
- breakout scores;
- fixed ATR gates;
- trend filters;
- arbitrary minimum departure thresholds.

---

## 13. PAPER-016 current authority

```text
Time: 2025-02-20 09:00
Direction: LONG
Entry: ~2946.8
Type: OPEN ROUTE continuation
Fixed TP: none
Working anchor: 2944-2945
Status: OPEN
Causal cutoff: 2025-02-20 10:30
```

Falsification:

```text
genuine 2944-2945 re-loss
+
failure to recover 2946-2947 business
```

`2947-2948` remains only a candidate memory at the cutoff.

Parent working belief:

```text
upper journey currently more coherent,
endpoint unknown,
child result cannot prove parent.
```

### S correction

Historical frozen note:

```text
S ≈ 9.90
```

Authoritative M1 recomputation using the established previous-completed H4 Wilder ATR14 convention:

```text
S = 14.26401609770702
```

The previous completed H4 period at the 09:00 entry is the bar beginning `04:00`.

Corrected descriptive values:

```text
entry -> working anchor distance ≈ 0.13-0.20S
entry -> MFE 2954.73        ≈ +0.56S
entry -> 10:30 2954.17      ≈ +0.52S
```

This is a coordinate correction only.

It does not change the already frozen trade decision.

---

## 14. Current exact next task

Do not change the protocol.

Continue PAPER-016 from:

```text
2025-02-20 10:30
```

Reveal only future data after the cutoff.

The next ordinary checkpoint may be:

```text
2025-02-20 10:45
```

At every reveal:

```text
TRADE / NO TRADE / NOT YET

if position open:
current child thesis
working anchor
whether anchor authority changed
whether falsification occurred
Parent Journey update only if materially changed
```

Do not use outcome knowledge.

Finish February before promoting any February-derived rule.

---

## 15. Research immediately after February

Two independent archaeology tracks.

### A. OPEN ROUTE entry matched pairs

Compare:

```text
006 / 010
vs
003 / 013 / 014 / 015
```

Keep PAPER-011 as a special positive/capture-latency case.

Study market role:

- genuine memory consumption vs traversal;
- actual new business/value formation;
- whether an anchor was truly earned;
- Parent Journey damage/repair state;
- whether the route was actually open;
- departure and pullback roles.

Do not optimize thresholds.

### B. Winner capture archaeology

Compare:

```text
006
010
011
```

Question:

> Can normal pullback and genuine continuation deterioration be distinguished sooner without trying to predict the top?

Entry and exit research must remain separate.

---

## 16. Causal replay instrumentation

Research script:

```text
scripts/v9_causal_m1.py
```

Tests:

```text
tests/test_v9_causal_m1.py
```

The harness is shadow research instrumentation.

It may:

- verify authoritative source SHA;
- create an exact revealed prefix;
- incrementally reveal data using a stored byte offset;
- verify revealed-prefix integrity;
- aggregate revealed data;
- calculate previous-completed H4 Wilder ATR14.

It must never:

- authorize a trade;
- set Entry/SL/TP;
- change sizing;
- change lifecycle;
- inspect post-cutoff data for discretionary decisions.

---

## 17. Mandatory anti-drift check

Before adding a rule ask:

1. Are we secretly solving direction instead of trade quality?
2. Are we converting useful language into supposed objective truth?
3. Are we fitting a tiny February sample?
4. Are we confusing a child outcome with Parent Journey state?
5. Are we requiring certainty a real trader would not need?
6. Are we calling a continuation attempt independent when it is really the same failed thesis?
7. Are we optimizing outcome rather than quality of attempt?

If yes, return to causal replay.

---

## 18. Formalization gate

Still closed.

Need substantially more evidence before any V9 EA contract:

- multiple complete future-hidden Parent Journeys;
- positive and negative child/parent combinations;
- stable distinction between child invalidation and parent change;
- stable OPEN ROUTE language across unseen examples;
- independent-session reproducibility;
- no outcome-by-outcome definition repair;
- later exact execution validation.

Production status:

```text
NONE
```