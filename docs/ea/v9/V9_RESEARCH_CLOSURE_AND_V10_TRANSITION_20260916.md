# V9 Research Closure and V10 Transition

Date: `2026-09-16`
Status: `V9 RESEARCH GENERATION CLOSED / PROTOTYPE PRESERVED AS HISTORICAL CONTROL / V10 SUCCESSOR ACTIVE`
Market: `GOLD# ONLY`
Transition base GitHub HEAD: `bebdfd73567e4bc5e311f739dbb359c388a8ac40`

## 1. What is being closed

V9 as an **active research generation** is closed at this checkpoint.

This does **not** mean the frozen V9 prototype was invalidated. The following remain preserved as historical controls and evidence:

- V9 Arrival / Delivery Grammar;
- causal H1 structural invalidation;
- H4 ATR180 era normalization;
- `ERA_RISK <= 4`;
- ANCHOR / CONTINUATION role semantics;
- actual-tick validation work;
- ALL_TO_CHALLENGE terminal shadow comparator;
- literal H4-HA terminal Oracle and terminal state-table research.

No V10 result silently rewrites V9 history.

## 2. Where V9 ended

The comparable terminal-shadow cohort was:

```text
693 Children
191 routes
BASE +7,210.31
PF 1.5655
DD 1,700.62
```

The corrected literal V9 exit answer sheet was:

```text
TRUE LAST ACCEPTED CHILD
-> first completed opposite-color H4 HA after that Child
-> close every still-open Child at actual H4_FINALIZED Bid/Ask

157 Oracle routes
ORACLE +19,827.81
PF 3.6176
DD 856.81
```

The 2025-2026 reporting slice was:

```text
450 Children
BASE +5,307.95
LITERAL ORACLE +16,476.18
```

The strongest consumed-data repeat-state terminal combination reached:

```text
SAME_PARTICIPATION_LOSS
+ OPP_PRESSURE_GAIN
+ CYCLE_MIN
+ RESILIENT_PRESSURE

PnL +9,528.95
BASE delta +4,221.00
Oracle recovery 37.79%
PF 2.103
DD 1,529.50
```

However, nested prior-only combination selection fell to:

```text
PnL +6,064.84
BASE delta +756.89
Oracle recovery 6.78%
PF 1.642
DD 2,344.84
```

Therefore V9 ended with a useful causal terminal-state vocabulary, but **without a stable terminal policy that justified promotion**.

## 3. Why the research question changed

The terminal research created a more fundamental question:

> If the desired economic shape is to accumulate exposure through a favorable HA run and exit near the first genuine opposite HA, why must Grammar remain the primary entry clock?

V9 Grammar entries are structurally meaningful, but by construction they often occur after a liquidity break / arrival and are not optimized for favorable entry price or repeated participation through a persistent directional run.

The HA-primary shadow work showed that the larger opportunity may be in **participation selection**, not only in exit timing.

The key diagnostic answer sheet became:

```text
PHA = same-color H4 HA run
k   = current completed HA index inside the run
L   = final run length, future-only answer-sheet information

EARLY-THIRD ORACLE:
3*k <= L
```

In the final M1-first-touch / H1-structural-risk research frame:

```text
FAST HA all eligible participation:
PnL +10,007.27
PF 1.368
DD 2,034.28
+253.72R

TRUE FAST early-third participation Oracle:
PnL +23,344.73
PF 15.157
DD 148.81
+733.18R
```

This participation ceiling is a different problem from the V9 exit Oracle and materially motivated a new generation.

## 4. Why this becomes V10 instead of silently changing V9

The center of authority is reversed.

V9 research architecture:

```text
Grammar -> entry / route semantics
HA and state -> terminal / exit research
```

V10 research architecture:

```text
HA -> primary participation / execution clock
Grammar -> structural context
STD / SLOW HA -> maturity / regime context
H1 liquidity -> Child structural invalidation / risk
state models -> ADD / STOP-ADDING / bounded exposure
FAST opposite HA -> default research exit comparator
```

This is a strategy-generation change, not a small V9 exit patch. It is therefore named **V10**.

## 5. What carries forward from V9

V10 inherits the research discipline, not an automatic production promotion:

- causal replay only;
- no future price in live features;
- accidental future reveal contaminates the interval;
- stopped Child remains dead;
- no hindsight resurrection;
- no hidden threshold, cooldown, retry limit, fixed no-chase rule, Child cap, or forced LONG/SHORT balance;
- structural H1 invalidation remains the current V10 screening baseline;
- `ERA_RISK <= 4` remains the current screening eligibility baseline;
- all currently studied 2024-2026 periods are consumed development evidence;
- actual-tick execution and forward evidence remain required before promotion.

## 6. Historical status

V9 is now a **closed research predecessor and frozen comparator**.

For active HA-primary work, resume from:

`docs/ea/v10/AGENTS_V10.md`
