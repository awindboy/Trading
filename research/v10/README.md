# V10 research code

Only retained scripts with a current, named purpose belong here. Generated ledgers and model-search outputs belong under ignored `output/`.

## R3 K1 danger reproduction

- `build_v10_causal_m1_universe_r3.py` — rebuild the causal raw-M1 opportunity universe.
- `tune_v10_r3_models.py` — historical R3 model comparison; consumed-data reproduction only.
- `probe_v10_r3_meta_danger.py` — purpose-specific danger diagnostics.
- `validate_v10_r3_k1stop_candidate.py` — validate the fixed K1 candidate.
- `compare_v10_r3_k1stop_specs.py` — compare fixed K1 specifications.
- `validate_v10_r3_stable_union.py` — diagnostic union validation.
- `ablate_v10_r3_k1stop_features.py` — feature ablation.
- `train_v10_r3_k1stop_future_shadow.py` — build the frozen future-shadow bundle.
- `publish_v10_r3_k1stop_results.py` — publish compact receipts.
- `validate_v10_r3_publication.py` — validate the compact R3 publication pack.
- `requirements-v10-r3-ml.txt` — pinned Python research dependencies.

These scripts reproduce historical research. They do not grant action authority.

## 2026-09-21 stop-reduction studies

- `analyze_v10_intrah4_next_ha_nowcast.py` — causal forming-H4 prefix study; broad exit authority rejected.
- `analyze_v10_intrah4_action_value.py` — asymmetric action-value diagnostic; rejected.
- `freeze_v10_intrah4_seed_defense_shadow.py` — freeze the narrow one-unit +60m future-shadow contract.
- `explore_v10_progression_ma_bands.py` — progression/damage-repair rejection and normalized MA-ribbon study.
- `prepare_v10_forward_m1_extension.py` — verify an MT5 M1 cache against the trusted prefix and write one chronological extended raw-M1 source.
- `build_v10_ma_band_forward_shadow.py` — stream raw M1 once and log the frozen normalized H4/H1/M15 MA-band state on post-cutoff R7G candidates.
- `analyze_v10_ma_band_forward_shadow.py` — audit each fixed MA coordinate on forward outcomes without fitting a threshold or composite score.

These scripts are retained because their purpose is explicit and their conclusions are current. Their large outputs are local and ignored.

## Removed code

Old Gemini January prototypes, bounded-m3 regeneration utilities, R2 live-model exports, reversal/regime probes, R4/R5 search grids, generated ledgers, and model snapshots were removed from the current tree. Git history is the archive.
